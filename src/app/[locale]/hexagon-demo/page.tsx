import React from 'react';
import Bounded from '@/components/Bounded';
import HexagonDemo from '@/components/demo/HexagonDemo';

export default function HexagonDemoPage() {
  return (
    <Bounded>
      <HexagonDemo />
    </Bounded>
  );
}